# Test Environment Configuration
# Copy this to .env.test for local testing

# Environment
NODE_ENV=test

# Test Database (MUST contain "test" in the name)
MONGODB_URI=mongodb://localhost:27017/nairobi-verified-test

# Test Secrets (different from production)
JWT_SECRET=test-jwt-secret-for-testing-only-not-production
SESSION_SECRET=test-session-secret-for-testing-only-not-production

# Session Configuration
SKIP_SESSION=false
SESSION_NAME=nairobi_verified_test_session

# Test-specific Settings
PORT=5001
FRONTEND_URL=http://localhost:3000

# Cloudinary (optional - tests use mock uploads)
# CLOUDINARY_CLOUD_NAME=test-cloud
# CLOUDINARY_API_KEY=test-key
# CLOUDINARY_API_SECRET=test-secret

# Email (optional - tests don't send real emails)
# EMAIL_FROM=test@example.com

# Redis (optional - session storage)
# REDIS_URL=redis://localhost:6379/1

# IMPORTANT NOTES:
# 1. This file is for LOCAL TESTING ONLY
# 2. MONGODB_URI MUST contain "test" in database name
# 3. Tests will FAIL if trying to use production database
# 4. Never commit real credentials to this file
# 5. CI/CD uses separate configuration in .github/workflows/
